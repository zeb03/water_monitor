text: 这是测试环境

# 数据库驱动
# 数据库连接地址
# 数据库用户名&密码
# 应用服务 WEB 访问端口
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/water_monitor?serverTimezone=UTC
    username: root
    password: abc123

server:
  port: 9999

